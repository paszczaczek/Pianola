<UserControl x:Class="Pianola.SignVisualTest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Pianola"
             mc:Ignorable="d" HorizontalAlignment="Left" VerticalAlignment="Top" VerticalContentAlignment="Top">
    <UserControl.Resources>
        <Style TargetType="local:Sign">
            <Setter Property="Margin" Value="2" />
            <Setter Property="VerticalAlignment" Value="Top" />
        </Style>
    </UserControl.Resources>

    <StackPanel Orientation="Horizontal">
        <local:Sign Text="{x:Static local:Sign.TrebleClef}" IsGuidLinesVisible="True" />
        <local:Sign Text="{x:Static local:Sign.BassClef}" IsGuidLinesVisible="True" />
        <local:Sign Text="{x:Static local:Sign.BlackNoteHead}" IsGuidLinesVisible="True" />
        <local:Sign Text="{x:Static local:Sign.WhiteNoteHead}" IsGuidLinesVisible="True" />
        <local:Sign Text="{x:Static local:Sign.Sharp}" IsGuidLinesVisible="True" />
        <local:Sign Text="{x:Static local:Sign.Flat}" IsGuidLinesVisible="True" />
        <local:Sign Text="{x:Static local:Sign.Natural}" IsGuidLinesVisible="True" />
    </StackPanel>
</UserControl>
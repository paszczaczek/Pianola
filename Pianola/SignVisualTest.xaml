<UserControl x:Class="Pianola.SignVisualTest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Pianola"
             mc:Ignorable="d" HorizontalAlignment="Left" VerticalAlignment="Top" VerticalContentAlignment="Top">
    <UserControl.Resources>
        <Style TargetType="local:Sign">
            <Setter Property="Margin" Value="2" />
            <Setter Property="VerticalAlignment" Value="Top" />
        </Style>
    </UserControl.Resources>

    <StackPanel Orientation="Horizontal">
        <local:Sign Text="{x:Static local:Sign.TrebleClef}" IsGridLinesVisible="True" />
        <local:Sign Text="{x:Static local:Sign.BassClef}" IsGridLinesVisible="True" />
        <local:Sign Text="{x:Static local:Sign.BlackNoteHead}" IsGridLinesVisible="True" />
        <local:Sign Text="{x:Static local:Sign.WhiteNoteHead}" IsGridLinesVisible="True" />
        <local:Sign Text="{x:Static local:Sign.Sharp}" IsGridLinesVisible="True" />
        <local:Sign Text="{x:Static local:Sign.Flat}" IsGridLinesVisible="True" />
        <local:Sign Text="{x:Static local:Sign.Natural}" IsGridLinesVisible="True" />
    </StackPanel>
</UserControl>
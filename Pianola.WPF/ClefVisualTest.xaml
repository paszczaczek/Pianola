<UserControl x:Class="Pianola.ClefVisualTest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Pianola"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style TargetType="local:Clef">
            <Setter Property="Margin" Value="2 20 2 40" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="IsGridlinesVisible" Value="True" />
            <Setter Property="IsStaffLinesVisible" Value="True" />
        </Style>
    </UserControl.Resources>

    <StackPanel Orientation="Horizontal">
        <local:Clef Type="Treble"/>
        <local:Clef Type="Bass"/>
    </StackPanel>
</UserControl>